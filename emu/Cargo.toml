[package]
name = "rustemu"
version = "0.1.0"
edition = "2024"

[dependencies]
vcs = { path = "../vcs" }
nes = { path = "../nes" }
bevy = { version = "0.18.1", default-features = false }
bevy_file_dialog = "0.9.1"

[profile.release]
debug = true
